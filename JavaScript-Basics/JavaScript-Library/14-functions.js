function calc (x, y, z){
    if (z === "+"){
        console.log (x + y);
    } else if (z === "-"){
        console.log (x - y)
    } else if (z === "*"){
        console.log (x * y)
    } else if (z === "/"){
        console.log (x / y)
    } else {
        console.log("Unpexted input")
    }

    let calc ("*",5,3)

}